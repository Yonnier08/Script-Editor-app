<?xml version="1.0" encoding="UTF-8" ?>
<TabbedPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:local="clr-namespace:ScriptEditorMaui"
            x:Class="ScriptEditorMaui.MainPage"
            Title="ScriptEditor"
            BarBackgroundColor="#1F1F1F"
            BarTextColor="White">

    <TabbedPage.BindingContext>
        <local:MainViewModel />
    </TabbedPage.BindingContext>

    <!-- PESTAÑA 1: SCRIPT -->
    <ContentPage Title="Script">
        <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="10" RowSpacing="8">[span_2](start_span)[span_2](end_span)

            <!-- MENÚS PRINCIPALES ESTILO PC -->
            <HorizontalStackLayout Grid.Row="0" Spacing="4" BackgroundColor="#2D2D2D" Padding="4">
                <Button Text="File" Command="{Binding MenuFileCommand}" BackgroundColor="Transparent" TextColor="White" />
                <Button Text="Edit" Command="{Binding MenuEditCommand}" BackgroundColor="Transparent" TextColor="White" />
                <Button Text="Export" Command="{Binding MenuExportCommand}" BackgroundColor="Transparent" TextColor="White" />
                <Button Text="Tools" Command="{Binding MenuToolsCommand}" BackgroundColor="Transparent" TextColor="White" />
                <BoxView WidthRequest="1" BackgroundColor="Gray" Margin="5,0" />
                <Label Text="{Binding StatusMessage}" VerticalOptions="Center" TextColor="LightGray" LineBreakMode="TailTruncation" FontSize="12" />[span_3](start_span)[span_3](end_span)
            </HorizontalStackLayout>

            <!-- OPCIONES RÁPIDAS DE CONFIGURACIÓN -->
            <HorizontalStackLayout Grid.Row="1" Spacing="16">[span_4](start_span)[span_4](end_span)
                <HorizontalStackLayout Spacing="4">[span_5](start_span)[span_5](end_span)
                    <CheckBox IsChecked="{Binding IsBigEndian}" />[span_6](start_span)[span_6](end_span)
                    <Label Text="Big Endian" VerticalOptions="Center" />[span_7](start_span)[span_7](end_span)
                </HorizontalStackLayout>[span_8](start_span)[span_8](end_span)
                <HorizontalStackLayout Spacing="4">[span_9](start_span)[span_9](end_span)
                    <CheckBox IsChecked="{Binding ToLower}" />[span_10](start_span)[span_10](end_span)
                    <Label Text="minúsculas" VerticalOptions="Center" />[span_11](start_span)[span_11](end_span)
                </HorizontalStackLayout>[span_12](start_span)[span_12](end_span)
                <HorizontalStackLayout Spacing="4">[span_13](start_span)[span_13](end_span)
                    <CheckBox IsChecked="{Binding ExcludeStageFilters}" />[span_14](start_span)[span_14](end_span)
                    <Label Text="Excluir filtros de stage" VerticalOptions="Center" />[span_15](start_span)[span_15](end_span)
                </HorizontalStackLayout>[span_16](start_span)[span_16](end_span)
            </HorizontalStackLayout>[span_17](start_span)[span_17](end_span)

            <!-- EDITOR CENTRAL -->
            <Editor Grid.Row="2"
                    Text="{Binding EditorText}"
                    AutoSize="TextChanges"
                    FontFamily="Courier"
                    Placeholder="Comandos DSC..." />[span_18](start_span)[span_18](end_span)

            <!-- ACCIONES RÁPIDAS INFERIORES -->
            <ScrollView Grid.Row="3" Orientation="Horizontal">[span_19](start_span)[span_19](end_span)
                <HorizontalStackLayout Spacing="6">[span_20](start_span)[span_20](end_span)
                    <Button Text="Add Target Flying Time" Command="{Binding AddTargetFlyingTimeCommandsCommand}" />[span_21](start_span)[span_21](end_span)
                    <Button Text="F2 → FT" Command="{Binding AutoFormatF2ToFtCommand}" />[span_22](start_span)[span_22](end_span)
                    <Button Text="FT → F2" Command="{Binding AutoFormatFtToF2Command}" />[span_23](start_span)[span_23](end_span)
                    <Button Text="FT → F2 (Vita)" Command="{Binding AutoFormatFtToF2VitaCommand}" />[span_24](start_span)[span_24](end_span)
                    <Button Text="Quitar chart" Command="{Binding RemoveChartCommand}" />[span_25](start_span)[span_25](end_span)
                    <Button Text="Reformat F Targets" Command="{Binding ReformatFTargetsCommand}" />[span_26](start_span)[span_26](end_span)
                    <Button Text="Reformat FT Targets" Command="{Binding ReformatFtTargetsCommand}" />[span_27](start_span)[span_27](end_span)
                    <Button Text="Quitar TIME sin uso" Command="{Binding RemoveUnusedTimeCommandsCommand}" />[span_28](start_span)[span_28](end_span)
                </HorizontalStackLayout>[span_29](start_span)[span_29](end_span)
            </ScrollView>[span_30](start_span)[span_30](end_span)

        </Grid>
    </ContentPage>

    <!-- PESTAÑA 2: PV-DATABASE -->
    <ContentPage Title="PV-Database">
        <Grid Padding="10" BackgroundColor="#0A0A0A">
            <Editor Text="{Binding PvDbText}"
                    FontFamily="Courier"
                    TextColor="White"
                    BackgroundColor="#121212"
                    IsReadOnly="True"
                    Placeholder="# Aquí aparecerá la estructura del pv_db.txt para FT..." />
        </Grid>
    </ContentPage>

    <!-- PESTAÑA 3: FIELD-DATABASE -->
    <ContentPage Title="Field-Database">
        <Grid Padding="10" BackgroundColor="#0A0A0A">
            <Editor Text="{Binding FieldDbText}"
                    FontFamily="Courier"
                    TextColor="White"
                    BackgroundColor="#121212"
                    IsReadOnly="True"
                    Placeholder="# Aquí aparecerá la estructura del field_db.txt para FT..." />
        </Grid>
    </ContentPage>

</TabbedPage>
